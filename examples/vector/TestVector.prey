@onlyUp
class
  @public
    @fun
      test: void
        @read
          vector = Vector<integer>(0, 1, 2, 3, 4)
          first = vector.get(0)

        print(`First element of vector: {first}`)
        vector.set(0, -1)
        print(`First element of vector after mutation: {vector.get(0)}`)
        print(`-------------`)
        print(vector)
